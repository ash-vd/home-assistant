- alias: Lightning - Bathroom - On
  trigger:
    - platform: state
      entity_id: input_boolean.bathroom_presence
      to: "on"

  action:
    - service: homeassistant.turn_on
      entity_id: light.shelly_dimmer_bathroom
      data:
        brightness: 100


- alias: Lightning - Bathroom - Standby
  trigger:
    - platform: state
      entity_id: input_boolean.bathroom_presence
      to: "off"

  action:
    - service: homeassistant.turn_on
      entity_id: light.shelly_dimmer_bathroom
      data:
        brightness: 25

- alias: Lightning - Bathroom - Off
  trigger:
    - platform: state
      entity_id: input_boolean.bathroom_presence
      to: "off"
      for:
        minutes: 5

  action:
    - service: homeassistant.turn_off
      entity_id: light.shelly_dimmer_bathroom
